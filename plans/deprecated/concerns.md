My collaborator has been working on the full chess side. We are training on the principal variation provided by LC0 (by iteratively making the best move) and the resulting model is able to do very with respect to playing strength. However, we are greatly suspecting that the decoder is cheating by decoding the next move prediction head and recursively doing this. This is because when we run some SFT training to see if the model is able to answer questions about the pieces, it is unable to do so with high accuracy (something that doesn't make sense since the model needs to understand the board to play chess well).
